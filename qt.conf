[Paths]
Plugins = plugins

# Tell the Qt Windows platform plugin to use Per-Monitor DPI awareness
# so AA_EnableHighDpiScaling can honor the Windows display scale (e.g. 150%).
[Platforms]
WindowsArguments = dpiawareness=2
